<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//sqlmap.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.polaris.common.mapper.UserMapper">
    <resultMap id="userResult" type="User">
        <result property="id" column="id"/>
        <result property="userName" column="user_name"/>
        <result property="password" column="pass_word"/>
        <result property="photo" column="photo"/>
        <result property="mobile" column="mobile"/>
        <result property="qq" column="qq"/>
    </resultMap>

    <select id="queryUser" parameterType="map" resultType="User">
        select *from user where user_name=#{userName} and pass_word=#{password} limit 1
    </select>
    <insert id="insert" parameterType="User">
        insert into user
        (user_name,pass_word,photo,mobile,qq)
        values
        (#{userName},#{password},#{photo},#{mobile},#{qq})
        <selectKey keyProperty="id" resultType="long">
            select last_insert_id()
        </selectKey>
    </insert>
</mapper>
